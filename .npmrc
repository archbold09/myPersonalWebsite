# pnpm configuration

# Require exact versions
save-exact=true

# Allow peer dependency mismatches
strict-peer-dependencies=false

# Ignore build scripts errors (for CI/CD compatibility)
ignore-scripts=false
